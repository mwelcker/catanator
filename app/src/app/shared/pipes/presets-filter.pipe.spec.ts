import {PresetsFilterPipe} from './presets-filter.pipe';

describe('PresetsFilterPipe', () => {
    it('create an instance', () => {
        const pipe = new PresetsFilterPipe();
        expect(pipe).toBeTruthy();
    });
});
